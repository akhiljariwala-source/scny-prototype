
  # SCNY User Verification Prototype

  This is a code bundle for SCNY User Verification Prototype. The original project is available at https://www.figma.com/design/putSUwSqw8JdSFLW64fUrl/SCNY-User-Verification-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  